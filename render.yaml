services:
  - type: web
    name: learning-notes
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /
